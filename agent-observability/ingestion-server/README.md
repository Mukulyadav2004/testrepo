# Ingestion Server

gRPC server that receives traces from the Python SDK via the `TraceIngestionService.IngestTrace`
RPC and publishes them onto a Redis Stream for downstream processing.

_Placeholder — service logic not yet implemented._
