# Lambda Evaluator

AWS Lambda LLM-as-judge evaluator. Triggered per trace, it uses Ollama to score the
trace and writes evaluation results back to PostgreSQL.

_Placeholder — service logic not yet implemented._
